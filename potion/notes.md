# Notes

- Initialized potion experiment folder.
- Initialized Go module with chi router and modernc sqlite driver.
- Added SQLite-backed Go HTTP API with chi router exposing CRUD for pages, links, and databases.
- Created Vite React frontend with markdown editor, database controls, and API integration.
- Added backend CORS middleware to allow frontend requests from browsers.
